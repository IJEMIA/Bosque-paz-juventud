/* Reset y estilos base */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    -webkit-tap-highlight-color: transparent;
}

body {
    font-family: 'Segoe UI', 'Poppins', 'Arial', sans-serif;
    background: linear-gradient(135deg, #1a472a 0%, #2d5a2c 50%, #3d7a3a 100%);
    min-height: 100vh;
    overflow-x: hidden;
    position: relative;
}

/* Contenedor principal del bosque */
.forest-container {
    max-width: 600px;
    margin: 0 auto;
    padding: 20px;
    min-height: 100vh;
    position: relative;
    z-index: 1;
}

/* Árboles de fondo decorativos */
.bg-trees {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 800"><path fill="%232d5a2c" opacity="0.1" d="M0,800 L0,600 Q100,550 200,600 Q300,650 400,600 Q500,550 600,600 Q700,650 800,600 Q900,550 1000,600 Q1100,650 1200,600 L1200,800 Z"/><path fill="%231a472a" opacity="0.1" d="M0,800 L0,650 Q150,600 300,650 Q450,700 600,650 Q750,600 900,650 Q1050,700 1200,650 L1200,800 Z"/></svg>');
    background-repeat: repeat-x;
    background-position: bottom;
    background-size: 300px;
    pointer-events: none;
    z-index: 0;
}

/* Header con contador de árboles */
.stats-header {
    background: rgba(30, 40, 20, 0.85);
    backdrop-filter: blur(10px);
    border-radius: 60px;
    padding: 12px 20px;
    margin-bottom: 20px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    border: 1px solid rgba(255,215,0,0.3);
}

.tree-counter {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    font-size: 1.5rem;
    color: #ffd966;
}

.tree-icon {
    font-size: 2rem;
    filter: drop-shadow(2px 2px 4px rgba(0,0,0,0.3));
}

#totalTrees {
    font-size: 2rem;
    font-weight: bold;
    color: #ffd700;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
}

.tree-label {
    font-size: 0.9rem;
    color: #e8e8e8;
}

/* Área del águila humanoide */
.eagle-area {
    margin-bottom: 30px;
}

.eagle-character {
    display: flex;
    align-items: center;
    gap: 15px;
    background: rgba(0,0,0,0.3);
    border-radius: 80px 80px 80px 30px;
    padding: 15px;
    backdrop-filter: blur(5px);
    animation: float 3s ease-in-out infinite;
}

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-5px); }
}

.eagle-avatar {
    flex-shrink: 0;
}

.eagle-avatar img {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid #ffd966;
    box-shadow: 0 4px 10px rgba(0,0,0,0.3);
    background: #2c5a2a;
}

/* Burbuja de diálogo */
.speech-bubble {
    position: relative;
    background: rgba(255,248,225,0.95);
    border-radius: 20px;
    padding: 15px 20px;
    flex: 1;
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    backdrop-filter: blur(5px);
}

.speech-bubble::before {
    content: '';
    position: absolute;
    left: -15px;
    top: 50%;
    transform: translateY(-50%);
    border-width: 10px 15px 10px 0;
    border-style: solid;
    border-color: transparent rgba(255,248,225,0.95) transparent transparent;
}

#eagleMessage {
    font-size: 0.95rem;
    line-height: 1.4;
    color: #2c3e2f;
    font-weight: 500;
    margin: 0;
}

/* Área del árbol en crecimiento */
.growing-tree-area {
    background: rgba(0,0,0,0.2);
    border-radius: 30px;
    padding: 20px;
    margin-bottom: 20px;
    text-align: center;
}

.tree-stage {
    text-align: center;
}

.tree-visual {
    font-size: 5rem;
    transition: all 0.3s ease;
    filter: drop-shadow(2px 4px 8px rgba(0,0,0,0.3));
}

.progress-bar-container {
    background: rgba(0,0,0,0.3);
    border-radius: 20px;
    height: 12px;
    margin-top: 15px;
    overflow: hidden;
}

.progress-bar {
    background: linear-gradient(90deg, #4caf50, #8bc34a);
    width: 0%;
    height: 100%;
    border-radius: 20px;
    transition: width 1s linear;
    box-shadow: 0 0 10px rgba(76,175,80,0.5);
}

/* Temporizador */
.timer-container {
    text-align: center;
    margin-bottom: 25px;
    background: rgba(0,0,0,0.4);
    border-radius: 40px;
    padding: 15px;
    backdrop-filter: blur(5px);
}

.timer-label {
    font-size: 0.9rem;
    color: #ffd966;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 5px;
}

.timer-display {
    font-size: 2.5rem;
    font-weight: bold;
    font-family: 'Courier New', monospace;
    color: #ffffff;
    text-shadow: 0 0 10px rgba(0,0,0,0.5);
    letter-spacing: 3px;
}

/* Botones de selección de tiempo */
.time-selection {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 12px;
    margin-bottom: 20px;
}

.time-btn {
    background: linear-gradient(135deg, #3d6b3a, #2c5a2a);
    border: none;
    padding: 15px 10px;
    border-radius: 50px;
    font-size: 1rem;
    font-weight: bold;
    color: #fff3cf;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    border: 1px solid rgba(255,215,0,0.3);
}

.time-btn:hover {
    transform: translateY(-2px);
    background: linear-gradient(135deg, #4c7a48, #3d6b3a);
    box-shadow: 0 6px 15px rgba(0,0,0,0.3);
    border-color: #ffd966;
}

.time-btn:active {
    transform: translateY(0);
}

/* Mensaje de estado */
.status-message {
    text-align: center;
    padding: 10px;
    font-size: 0.9rem;
    color: #ffd966;
    min-height: 50px;
}

/* PANTALLA DE BLOQUEO */
.lock-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #1a2a1a, #0e1a0e);
    z-index: 1000;
    display: none;
    justify-content: center;
    align-items: center;
    backdrop-filter: blur(10px);
    animation: fadeIn 0.5s ease;
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

.lock-content {
    text-align: center;
    color: white;
    padding: 30px;
    background: rgba(0,0,0,0.7);
    border-radius: 50px;
    border: 2px solid #ffd966;
    max-width: 90%;
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0%, 100% { box-shadow: 0 0 20px rgba(255,217,102,0.3); }
    50% { box-shadow: 0 0 40px rgba(255,217,102,0.6); }
}

.lock-icon {
    font-size: 4rem;
    margin-bottom: 15px;
}

.lock-content h2 {
    color: #ffd966;
    margin-bottom: 10px;
}

.lock-content p {
    margin-bottom: 15px;
    font-size: 1rem;
}

.lock-timer-mini {
    font-size: 2rem;
    font-family: monospace;
    background: rgba(0,0,0,0.5);
    padding: 10px;
    border-radius: 20px;
    margin: 15px 0;
    letter-spacing: 2px;
}

.warning-text {
    color: #ff6b6b;
    font-size: 0.8rem;
    margin-top: 10px;
    animation: blink 1s infinite;
}

@keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.6; }
}

/* MODAL DE ADVERTENCIA */
.modal {
    display: none;
    position: fixed;
    z-index: 2000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.8);
    justify-content: center;
    align-items: center;
}

.modal-content {
    background: linear-gradient(135deg, #2d2b1f, #1e2a1a);
    margin: auto;
    padding: 30px;
    border-radius: 30px;
    width: 90%;
    max-width: 350px;
    text-align: center;
    border: 2px solid #ffd966;
    position: relative;
    animation: slideDown 0.3s ease;
}

@keyframes slideDown {
    from {
        transform: translateY(-50px);
        opacity: 0;
    }
    to {
        transform: translateY(0);
        opacity: 1;
    }
}

.close-modal {
    position: absolute;
    right: 20px;
    top: 15px;
    font-size: 1.8rem;
    cursor: pointer;
    color: #ffd966;
}

.close-modal:hover {
    color: #ff6b6b;
}

.modal-content h3 {
    color: #ffd966;
    margin-bottom: 15px;
}

.modal-content p {
    color: #e8e8e8;
    margin-bottom: 25px;
}

.danger-btn {
    background: #c44536;
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 40px;
    cursor: pointer;
    margin: 5px;
    font-weight: bold;
    transition: all 0.3s;
}

.danger-btn:hover {
    background: #a53528;
    transform: scale(1.05);
}

.safe-btn {
    background: #3d6b3a;
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 40px;
    cursor: pointer;
    margin: 5px;
    font-weight: bold;
    transition: all 0.3s;
}

.safe-btn:hover {
    background: #2c5a2a;
    transform: scale(1.05);
}

/* Responsive Design */
@media (max-width: 480px) {
    .forest-container {
        padding: 15px;
    }
    
    .eagle-character {
        flex-direction: column;
        text-align: center;
        border-radius: 40px;
    }
    
    .speech-bubble::before {
        left: 50%;
        top: -15px;
        transform: translateX(-50%);
        border-width: 0 10px 15px 10px;
        border-color: transparent transparent rgba(255,248,225,0.95) transparent;
    }
    
    .time-selection {
        grid-template-columns: 1fr 1fr;
    }
    
    .timer-display {
        font-size: 1.8rem;
    }
    
    .tree-visual {
        font-size: 3.5rem;
    }
    
    .eagle-avatar img {
        width: 70px;
        height: 70px;
    }
}

/* Efecto de partículas (opcional) */
.forest-container::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 20% 40%, rgba(255,217,102,0.05) 0%, transparent 50%);
    pointer-events: none;
    z-index: 0;
}

/* Animación para el árbol cuando crece */
@keyframes grow {
    0% { transform: scale(0.8); opacity: 0.5; }
    100% { transform: scale(1.2); opacity: 1; }
}

.tree-stage.grow-animation .tree-visual {
    animation: grow 0.5s ease;
}

/* Estilo para botones deshabilitados */
.time-btn.disabled {
    opacity: 0.5;
    cursor: not-allowed;
    transform: none;
}

.time-btn.disabled:hover {
    transform: none;
    background: linear-gradient(135deg, #3d6b3a, #2c5a2a);
}

/* Scroll suave */
html {
    scroll-behavior: smooth;
}

/* Selección de texto */
::selection {
    background: #ffd966;
    color: #1a472a;
}
